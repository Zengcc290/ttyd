# Webterm auth/proxy block. Included by openresty host configs.
# Paths assume files live under /opt/webterm and are mounted into openresty as needed.

# NOTE: openresty container cannot see /opt/webterm by default.
# We keep active conf under /opt/1panel/www and store canonical copies here.
